// {{CHAPTER}} / {{PROBLEM}}
//
// 링크: https://algospot.com/judge/problem/read/{{PROBLEM}}
//
// 이 문제를 파이썬으로만 풀 거면 이 파일을 지우면 된다
// — make test 가 조용히 건너뛴다.

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 종만북/algospot 은 첫 줄이 테스트 케이스 개수.
    int C;
    if (!(cin >> C)) return 0;
    while (C--) {
        // TODO: 케이스 하나를 읽고 풀어서 출력
        //
        //   int n; cin >> n;
        //   vector<string> words(n);
        //   for (auto& w : words) cin >> w;
        //   cout << answer << '\n';
        //
        // 디버그는 cerr 로 — cout 은 채점 대상이다.
    }

    return 0;
}
